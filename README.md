Used Vite to create a starter React app.
Main code is in `src/App.tsx`


Install packages with `npm install`

Run with `npm run dev`

Can also open from a link: https://rytismark.github.io/tree-view/
